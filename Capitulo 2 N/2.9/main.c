#include <stdio.h>
#include <stdlib.h>
void main(void)
{
float RES;
int R, T, Q;
printf("Ingrese los valores de R, T y Q: ");
scanf("%d %d %d", &R, &T, &Q);
if (RES < 820)
printf("\nR = %d\tT = %d\t Q = %d", R, T, Q);
}
